name: platform_abstraction
type: core
version: 0.1.0
tech_stack:
  - rust

dependencies:
    - shared_types

estimated_size:
  lines_of_code: 12000
  tokens: 120000

quality_requirements:
  min_test_coverage: 80
  enforce_tdd: true
  enforce_bdd: false

description: Platform-specific window management implementations for Linux, Windows, and macOS
