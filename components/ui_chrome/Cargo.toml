[package]
name = "ui_chrome"
version = "0.17.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
# Test dependencies will be added as needed

[[test]]
name = "unit_tests"
path = "tests/unit/mod.rs"
